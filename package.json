{
  "name": "cosmikit-figma",
  "version": "0.1.0",
  "description": "Cosmikit — Design System Sync for Figma",
  "main": "dist/code.js",
  "scripts": {
    "dev": "webpack --mode=development --watch",
    "build": "webpack --mode=production",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "release": "npm run build && mkdir -p release && cp manifest.json release/ && cp -r dist release/ && cd release && zip -r ../cosmikit-figma-plugin.zip . && cd .. && rm -rf release"
  },
  "dependencies": {
    "preact": "^10.24.0"
  },
  "devDependencies": {
    "@figma/plugin-typings": "^1.104.0",
    "css-loader": "^7.1.0",
    "html-inline-script-webpack-plugin": "^3.2.1",
    "html-webpack-plugin": "^5.6.0",
    "style-loader": "^4.0.0",
    "ts-loader": "^9.5.1",
    "typescript": "^5.6.0",
    "vitest": "^4.0.18",
    "webpack": "^5.96.0",
    "webpack-cli": "^5.1.4"
  }
}
