# ===========================================
# Cosmikit Web Dashboard — Environment Variables
# ===========================================

# NextAuth.js
AUTH_SECRET=              # openssl rand -base64 32
AUTH_GITHUB_ID=           # GitHub OAuth App client ID
AUTH_GITHUB_SECRET=       # GitHub OAuth App client secret

# Database (Vercel Postgres / Neon)
DATABASE_URL=             # postgresql://user:pass@host:5432/dbname

# Encryption (for API keys at rest)
ENCRYPTION_KEY=           # 32-byte hex key: openssl rand -hex 32

# Plugin JWT
PLUGIN_JWT_SECRET=        # openssl rand -base64 32

# App URL
NEXT_PUBLIC_APP_URL=http://localhost:3000  # Change to production URL when deployed
